# Gemini is optional. Without it, the API returns safe local fallback replies.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Medicines are loaded from this file only. No internal fallback medicine data is used.
MEDICINES_DATA_PATH=egypt_common_100_medicines_chatbot.csv
